<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Coingecko Assessment</title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="A production ready example Rails app that's using Docker and Docker Compose.">

    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload" %>

    <% # Generated with: https://realfavicongenerator.net/ %>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#61b9d2">
    <meta name="msapplication-TileColor" content="#da532c">
    <meta name="theme-color" content="#ffffff">
  </head>

  <body class="flex flex-col min-h-screen bg-gray-100">
    <nav class="bg-green-600 text-white shadow-md">
      <div class="max-w-screen-xl mx-auto px-4 py-2 flex items-center justify-between">
        <a href="#" class="text-2xl font-semibold">Coingecko Assessment</a>
        <div class="space-x-4">
          <%= link_to "Home", root_path, class: "hover:bg-green-700 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Swap", swap_path, class: "hover:bg-green-700 px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "MANA", mana_path, class: "hover:bg-green-700 px-3 py-2 rounded-md text-sm font-medium" %>
        </div>
      </div>
    </nav>

    <main class="flex-grow container mx-auto py-4">
      <%= yield %>
    </main>

    <footer class="bg-green-600 text-white text-center py-3">
      <p class="mb-0">Coingecko Assessment &copy; <%= Time.now.year %></p>
    </footer>
  </body>
</html>
